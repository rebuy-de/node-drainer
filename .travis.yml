language: go

services:
- docker

before_install:
- docker pull quay.io/rebuy/rebuy-go-sdk:v2.4.0

script:
- docker run
    --rm -t
    -v $TRAVIS_BUILD_DIR:/node-drainer
    -w /node-drainer
    quay.io/rebuy/rebuy-go-sdk:v2.4.0
    buildutil -v
